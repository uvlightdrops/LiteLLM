stringData:
  litellm-master-key: "${LITELLM_MASTER_KEY_ENV}"
  database-password: "${DATABASE_PASSWORD_ENV}"
  database-url: "${DATABASE_URL_ENV}"
  openai-api-key: "${OPENAI_API_KEY_ENV}"
  anthropic-api-key: "${ANTHROPIC_API_KEY_ENV}"
  cohere-api-key: "${COHERE_API_KEY_ENV}"
  azure-api-key: "${AZURE_API_KEY_ENV}"
  azure-endpoint: "${AZURE_ENDPOINT_ENV}"
  jwt-secret: "${JWT_SECRET_ENV}"
  jwt-algorithm: "HS256"
  smtp-host: "${SMTP_HOST_ENV}"
  smtp-port: "${SMTP_PORT_ENV}"
  smtp-username: "${SMTP_USERNAME_ENV}"
  smtp-password: "${SMTP_PASSWORD_ENV}"
  smtp-from-email: "${SMTP_FROM_EMAIL_ENV}"
  sentry-dsn: "${SENTRY_DSN_ENV}"
  datadog-api-key: "${DATADOG_API_KEY_ENV}"
